---
    - hosts: all
      become: true
      tasks:
          - name: install common applications
            apt: name={{item}} state=present update_cache=yes cache_valid_time=86400
            changed_when: false
            with_items:
                 - ntp
                 - tree
                 - curl
